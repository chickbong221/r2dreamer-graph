# Task-specific settings. The camera set, the proprioception width and the
# graph capacities come from the dataset's metadata -- they are recorded there
# by the collector, and this file must not restate them.
task:
  env_id: PlaceSphere-v1
  instruction: "place the sphere into the bin"
  dataset: ${data.root}/PlaceSphere-v1/${data.name}
