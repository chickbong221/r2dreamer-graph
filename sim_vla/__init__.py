"""Simulator-side VLA experiment: data collection, envs and training.

Separate from ``scenegraph`` and ``envs`` on purpose -- this experiment supplies
its own configuration and must not change the simulator code the existing
Dreamer arms run against.
"""
