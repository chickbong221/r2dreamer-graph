 38 
A full DDIM sampling process recovers a clean sample 𝑥! by chaining the sub-policies {𝜋9(𝑥6,-.|𝑥6,,𝜏G→𝜏G-.)}G0A., starting from 𝑥6&∼𝒩(0,𝐈). In practice, one may set 𝜎"→J=0 for fully deterministic sampling or choose a small positive 𝜎"→J (subject to Equation (S19)) to tradeoff between sample diversity and stability. If the log-likelihood in Equation (S21) is later used as an objective or as part of a fine-tuning criterion, care must be taken in handling the 𝜎"→J→0 limit (e.g., by restricting likelihood-based terms to steps with strictly positive variance). Notation and scheduling conventions. Throughout the paper we distinguish MDP timesteps from diffusion (denoising) timesteps. Environmental timesteps are denoted by 𝑡, while diffusion indices follow a (possibly subsampled) schedule 𝜏A>𝜏A-.>⋯>𝜏., and are written as superscripts (e.g. 𝑎6,). A generic denoising transition from time 𝑡 to an earlier time 𝑚 is denoted by 𝑡→𝑚 (for the subsampled schedule this will typically be written 𝜏G→𝜏G-.. Variance parameters are indexed consistently as 𝜎6,→6,-. (abbreviated as 𝜎6, when unambiguous). We always enforce the constraint: 0≤𝜎6,≤1−𝛼6,-., (S22) so that all square roots appearing in the DDIM updates are real. When 𝜎6,=0 the corresponding transition degenerates to a deterministic mapping (Dirac), and Gaussian log-densities are not defined; therefore, any likelihood-based objective (e.g., policy-gradient using log𝜋) must only use steps with strictly positive variance. Consistency Models Consistency models (12) learn a single-step mapping from noisy inputs at arbitrary noise levels to clean data. Denote the consistency model by 𝐶9(𝑥6,𝜏) where the superscript indicates the diffusion index (per the notation above). Given a frozen diffusion teacher Ψh (for instance a 𝐾-step DDIM teacher that follows the same subsampled schedule {𝜏G}, consistency distillation minimizes the squared regression objective ℒZ[(𝜃)=𝔼f#,6,@ 𝐶9(𝑥6,𝜏)−sgFΨh(𝑥6,𝜏→0)H //¢, (S23) where sg[⋅] denotes stop-gradient and Ψh(𝑥6,𝜏→0) is the teacher's output after running the teacher's denoising chain from 𝑥6 down to (approximate) 𝑥!. The teacher must be run using the same subsampled schedule 𝜏G that the student will emulate or distill from. At inference time a consistency model requires only a single evaluation: 𝑥!≈𝐶9(𝑥6&,𝜏A),𝑥6&∼𝒩(0,𝐈). (S24) Diffusion Policy and RL Fine-tuning Diffusion Policy (3) performs diffusion over robot actions conditioning on observations. Given an observation 𝑜, we roll out along the 𝐾-step subsampled schedule {𝜏A>⋯>𝜏.}: 𝑎6,-.=𝑓9(𝑎6,,𝜏G|𝑜),𝑘=𝐾,…,2, (S25) where 𝑓9 follows the DDIM schedule as in Equation (S17) and (S18) conditioned on 𝑜. Then we can retrieve a clean action 𝑎"∶=𝑎6# from the so-called diffusion policy. RL formulation. Now we embed the 𝐾-step diffusion process as a sub-MDP into a single step in robotic manipulation MDP (28). Each denoising step can be viewed as sampling a cleaner noisy action 𝑎6,-. from the Gaussian sub-policy in Equation (S20). We therefore model the process as a 𝐾-step sub-MDP with: • Initial state: 𝑠A=(𝑎6&,𝜏A,𝑜) with 𝑎6&∼𝒩(0,𝐈). 