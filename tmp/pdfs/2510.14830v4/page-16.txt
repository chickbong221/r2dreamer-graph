 16 
of RL-100 loss (extended from PPO) is used to update the policy in both offline and online stages. (B) A policy can be factorized into an observation encoder with a diffusion-based actor. We propose regularization techniques (reconstruction and variational information bottleneck) to stabilize post-training. (C) We design a consistency-model distillation to enable one-step action generation and high-frequency control at deployment. Imitation Learning We initialize the policy by behavior cloning on human-teleoperated trajectories. Our approach uses conditional diffusion to learn robust visuomotor policies from demonstrations. Each episode provides synchronized tuples. {(𝑜",𝑞",𝑎")}"0.1!, (1) where 𝑜" are visual observations (RGB images or 3D point clouds), 𝑞" denotes robot proprioception (joint positions/velocities, gripper state), and 𝑎" is either a single-step action or an action chunk. Conditioning and prediction horizon. We fuse recent observations into a conditioning vector: 𝑐"=[𝜙(𝑜2,𝑞2)]20"-3"4.", (2) where the perception encoder 𝜙(⋅) processes the most recent 𝑛5 frames (typically 𝑛5=2) and [⋅] is the operator concatenating multiple vectors. The clean diffusion target 𝑎"6# at timestep 𝑡 is set to a single action 𝑎"6#=𝑢"∈ℝ7$ or an action chunk 𝑎"6#=F𝑢",…,𝑢"43%-.H∈ℝ3%7$  where 𝑛8 is the chunk size (typically 8-16). Actions are normalized per dimension; we predict delta end-effector pose when applicable. Diffusion parameterization. Following conditional diffusion over actions, we corrupt 𝑎"6# to 𝑎"6& via the forward process (Equation (S2)) The denoiser 𝜀9(𝑎6,𝜏,𝑐") is trained with the noise-prediction objective: ℒIL(𝜃)=𝔼(;'#,8()∼𝒟,6,@[‖𝜀−𝜀9(𝑎6,𝜏,𝑐")‖//], (3) where 𝒟 is the demonstration dataset, 𝜏∈{𝜏A>⋯>𝜏.} indexes a 𝐾-step schedule, and 𝜀~𝒩(0,𝐈). The policy backbone is shared across control modes; only the output head differs: ℝ7$ (single-step) or ℝ3%7$ (chunked). Vision and proprioception encoders. For RGB input, we use pretrained ResNet/ViT backbones; for point clouds input, we adapt the 3D encoder of DP3 (4) with reconstruction regularization for stability during RL fine-tuning. Visual embeddings are projected and concatenated with proprioceptive features to form 𝑐". All encoders are trained end-to-end with Equation (3). When applicable, we add reconstruction (Recon) and variational information bottleneck (VIB) regularization: ℒrecon=𝛽recon(𝑑Chamfer(𝑜V,𝑜)+‖𝑞V−𝑞‖//),	 (4) ℒKL=𝛽KLKL(𝜙(𝑧|𝑜,𝑠)∥𝒩(0,𝐈)),	 (5) where 𝑜 and 𝑞 denote the observed point cloud and proprioceptive vector; 𝑜V and 𝑞V are reconstructed observations given encoded embedding 𝜙(𝑜,𝑞); 𝑑Chamfer is Chamfer distance between two set of point clouds. The complete imitation learning objective becomes: ℒtotalIL=ℒIL+ℒrecon+ℒKL. (6) During RL fine-tuning, we reduce 𝛽B&*$C and 𝛽KL by a factor of 10 to allow policy improvement while maintaining representation stability. Inference and Control. At deployment, 𝐾-step DDIM sampling (Equation (S5)) generates actions: 